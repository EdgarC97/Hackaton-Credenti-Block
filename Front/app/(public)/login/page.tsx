import React from "react";
import LoginForm from "@/Front/app/components/LoginForm";

export const metadata = {
  title: "Login",
};

export default function LoginPage() {
  return (
    <div>
      <LoginForm />
    </div>
  );
}
